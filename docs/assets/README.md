# README preview assets

These PNG files are GitHub-friendly previews generated from the vector PDF
figures in `../../figures/`.

They are not independent analysis outputs. To regenerate the vector originals,
run:

```bash
python scripts/generate_cosmology_figures.py
```

The PNG previews can be regenerated from the PDFs with Poppler:

```bash
python scripts/render_readme_assets.py
```
